version: 2
jobs:
  build:
    docker:
      - image: circleci/python:2.7
    steps:
      - checkout
      - run:
          name: Install tox
          command: pip install tox
      - run:
          name: Test with tox
          command: tox
